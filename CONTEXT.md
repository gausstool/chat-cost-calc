# 术语定义（CONTEXT）

本文件记录项目中使用的核心领域术语与统一语言，便于团队理解与维护。

## 会话层级

| 术语 | 英文 | 定义 |
|---|---|---|
| **会话** | Session | 一次完整的对话周期，由多轮对话组成。默认每天 1 次会话 |
| **轮** | Turn / Round | 会话中的一次用户-助手交互回合。每轮包含用户输入、可能的工具调用、最终回复。默认每会话 22 轮 |
| **步** | Step | 每轮中的一次 LLM 调用。一轮可能包含多个步：初始用户消息调用 + N 个工具结果调用 + 1 个最终回复调用 |
| **调用** | Call | 一次具体的 LLM API 请求。每步对应一次调用 |

## Token 类型

| 术语 | 英文 | 定义 |
|---|---|---|
| **命中缓存输入** | Cache Hit Input | 请求中与上次请求相同、可复用缓存前缀的部分（固定前缀 + 历史上下文），按缓存价格计费 |
| **未命中缓存输入** | Cache Miss Input | 本次新增部分（用户消息 / 工具结果回传）+ 缓存失效时的全部前缀，按未命中价格计费 |
| **输出** | Output | LLM 生成的所有内容：工具调用（tool_use）、思考（reasoning）、正文回复，按输出价格计费 |
| **系统提示词** | System Prompt | 每轮请求携带的常驻上下文前缀，可被缓存，默认 3000 Token |
| **工具定义** | Tool Definitions | 工具的 JSON Schema 描述，随请求携带，可被缓存 |
| **思考** | Reasoning / Thinking | LLM 每次调用产生的推理内容。携带 tools 时按拼接规则进入上下文并参与缓存；不携带时仅计输出费用 |

## 每轮明细（Per-Round Detail）

每轮对话产生的数据结构：

```typescript
interface RoundData {
  round: number;           // 轮次 (1-based)
  cacheHit: number;        // 该轮累计命中缓存输入 Token（期望值）
  cacheMiss: number;       // 该轮累计未命中缓存输入 Token（期望值）
  output: number;          // 该轮累计输出 Token（工具调用 + 思考 + 正文）
  cost: number;            // 该轮费用（￥）
  cumulativeContext: number; // 会话至此的累计上下文长度
}
```

## 缓存模型

| 术语 | 定义 |
|---|---|
| **缓存概率** (p) | 每一步 LLM 调用缓存命中的概率。默认 0.99（即 1% 概率整段缓存失效） |
| **期望值口径** | 考虑缓存概率后的加权平均计算方式，非二元命中/未命中 |
| **固定前缀** | 系统提示词 + 工具定义的总和，每轮请求都携带 |
| **上下文** | 会话内已累积的历史输入 + 历史输出（含思考，当携带 tools 时） |

## 价格参数

| 术语 | 单位 | 默认值 |
|---|---|---|
| 缓存命中价格 | ￥/百万 Token | 0.05 |
| 缓存未命中价格 | ￥/百万 Token | 1.5 |
| 输出价格 | ￥/百万 Token | 4.5 |
| 高峰时段倍率 | × | 2（开启时所有价格翻倍） |
